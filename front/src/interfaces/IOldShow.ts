export interface ISshows {
    url: string // url para escuchar el show no se puede reproducir desde nuestra web
    name: string;
    image: string; // 645x645
    duration: number; //ya esta en horas
}